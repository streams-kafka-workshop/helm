{{- range untilStep 0 (.Values.loop.count | int64 | int) 1 }}
{{ $loop := printf "%s%s" $.Values.loop.prefix ((add . 1) | toString) }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $.Values.amqstreams.applicationName }}-{{ $loop }}
  namespace: {{ $.Values.amqstreams.applicationNamespace }}-{{ $loop }}
  annotations:
    {{- include "workshop.amqstreams.argocd-syncwave" $ | nindent 4 }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    name: ""
    namespace: {{ $.Values.amqstreams.destinationNamespace }}-{{ $loop }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ $.Values.amqstreams.source.repoUrl }}
    targetRevision: {{ $.Values.amqstreams.source.targetRevision }}
    path: {{ $.Values.amqstreams.source.path }}
    helm:
      valuesObject:
        amq-streams-console: 
          hostName: amq-streams-console-{{ $loop }}.{{ $.Values.subDomain }}          
{{- end }}